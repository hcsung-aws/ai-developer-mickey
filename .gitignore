# Mickey graph viz tool artifacts (M34)
scripts/mickey_graph/vendor/
scripts/output/

# Python cache
__pycache__/
*.py[cod]
.pytest_cache/

# v10 migration backups (Phase 0)
*.pre-v10-bak.zip
*.pre-v10-bak/

# Kiro CLI workspace state
.kiro/

# Serena LSP memories (per-machine, per-project)
.serena/

# apply 스크립트가 생성하는 로컬 백업 (git 이력이 원본을 담보하므로 추적 불필요)
*.bak-*
*.m[0-9]*-bak

# Curator 작업 영역 (staging + 락 + 리포트) — 디스크에서만 관리, 커밋 대상 아님
_curator-staging/
