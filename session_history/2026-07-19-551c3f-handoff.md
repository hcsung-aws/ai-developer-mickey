# HANDOFF — 2026-07-19 [UID: 551c3f] (mickey-power 트랙)

> 다음 세션 진입 문서. 트랙 정체는 `session_history/2026-07-16-mickey-power-kickoff.md` 참조 (정적 진입 정본 — §2가 이번 세션에서 개정됨).
> 세션 페어: 상세 경위는 `session_history/2026-07-19-551c3f-log.md` 참조.
> 인계 출처: `2026-07-17-b3d7e1-handoff.md` (1·2순위 전부 완료, 3순위 잔존)

## Current Status (한 줄)

b3d7e1 계획의 1순위(규약 성문화) + 2순위(hook 삭제·전면 개정) **완료 + 전 항목 검증 통과 + 커밋 `286cb91` push 완료**. 남은 것은 다음 세션 CLI 실증 + IDE 런타임 실증 (사용자 IDE 필요).

## 이번 세션 완료 사항 (전부 실측 검증됨)

1. **규약 성문화** — kickoff §2를 D-0717-1 전문으로 교체, DECISIONS.md에 D-0717-1/D-0717-2 기록, 2026-02-19 hook 결정 Status → 무효(Superseded). §6 acp-client 발화 사실도 갱신.
2. **UID 발급기 신설** — `.kiro/scripts/gen_session_uid.py` (session_history 내 충돌 회피 내장)
3. **legacy hook 2종 삭제** — `mickey-pre-task.kiro.hook`, `mickey-post-task.kiro.hook` (D-0717-2 집행)
4. **boot/close 스크립트 개정** — 소문자 `-handoff.md`/`-log.md` glob (크로스 플랫폼), UID 파싱·표기, "Mickey 1 취급" 오판 guidance 제거 (D-0717-1 양쪽 분기로 교체), `sys.stdout.reconfigure(encoding="utf-8")` (mojibake 해소 — U+FFFD 치환 관찰로 소비자가 UTF-8 디코딩임을 실측 판정)
5. **mickey-session-start.json 재작성** — v1 JSON 전체 필드 명시 (timeout 60, enabled true). CLI v3 / IDE 1.0 공용.
6. **verify_hooks.py 전면 개정** — 항목3 반전(legacy 부재 가드), 항목7 신설(D-0717-1 규약 계약). 개정 중 검증기가 주석 속 stale 리터럴을 잡아냄 (가드 동작 입증).
7. **README.md 전면 개정** — 폐기 이력 2건(07-19 legacy / 07-15 stop) + 인코딩 계약 성문화

## 검증 기준값 (2026-07-19 실측 — 다음 세션의 기준)

- `python scripts/verify_hooks.py` → **7/7** (새 기준값. 구 6/6은 폐기 — WELC 이력은 log T4 참조)
- `python scripts/verify_power_structure.py` → **7/7** (유지)
- `python scripts/verify_serving_sync.py` → **IN-SYNC (9파일)** (유지)

## 다음 세션 작업 계획 (우선순위)

### 완료됨 — 커밋/푸시 (세션 내 처리)

커밋 `286cb91` push 완료 (12파일, 433+/107-). `git commit -F <임시파일>` 방식 사용 — 이 환경(acp-client) 셸에서 `-m "..."` 큰따옴표가 벗겨지기 때문 (b3d7e1 2회 + 이번 세션 `;` 구분자 오인도 동일 계열 — 명령 분리 실행 필수).

### 1순위 — 다음 CLI 세션 실증 (새 세션 시작만으로 자동 수행됨)

새 kiro-cli/acp-client 세션 시작 시 boot hook 리포트를 관찰:
- [ ] 한글 guidance가 깨지지 않고 수신되는가 (UTF-8 고정 실증 — 이전엔 U+FFFD로 전멸)
- [ ] `[BRANCH: HANDOFF]`에 `uid: 551c3f`가 표기되는가 (UID 파싱 실증)
- [ ] guidance가 "Mickey 1 취급"이 아닌 D-0717-1 이어가기 절차인가 (오판 제거 실증)
- [ ] Mickey가 자동 진행하지 않고 사용자 확인을 요청하는가 (CLI 분기 준수)
- 하나라도 실패 시: log의 T6 근거(소비자 UTF-8 디코딩 가설) 재검토

### 2순위 — IDE 런타임 실증 (b3d7e1 3순위 승계, 사용자 IDE 필요)

체크리스트 (b3d7e1 원안 + 이번 개정 반영):
- IDE 1.0에서 이 workspace 열기 → Agent Hooks 패널이 `mickey-session-start.json`(v1 JSON)을 인식하는지
- `SessionStart` hook이 IDE에서 발화하고 stdout이 컨텍스트로 유입되는지 — **한글이 깨지지 않고 수신되는지** (이번 UTF-8 고정의 실증)
- boot 리포트의 HANDOFF 분기가 최신 handoff(UID 포함)를 제시하는지
- power-mickey가 IDE Powers 패널에서 인식/활성화되는지 (keyword 트리거 + steering pull)
- 결과를 log에 기록하고 기준값화

### 3순위 — 별건 관찰 처리 (log "관찰" 섹션 승계)

1. verify_power_structure.py / verify_serving_sync.py 콘솔 출력 cp949 mojibake — 같은 `reconfigure` 처방 적용 가능 (hook 아닌 직접 실행 경로)
2. `~/.kiro/mickey/_curator-staging` 대기 존재 — Pre-staged Apply 검토
3. Curator 호출 여부 판단 — 이번 세션도 full 정리 절차는 미수행

## Important Context (log/kickoff에 없는 것)

- 새 세션 boot 리포트는 이제 **한글 정상 + UID 표기**로 수신될 것 — 만약 여전히 깨지면 소비자 인코딩 가설 재검토 필요 (첫 실증 기회)
- CLI 이어가기 절차 준수: boot가 이 handoff를 제시하면 **사용자 확인 후** 진행 (자동 진행 금지)
- 검증기 항목 7 주의: boot/close 소스에 대문자 HANDOFF glob 리터럴을 다시 쓰면 (주석 포함) FAIL — 의도된 가드

## Quick Reference

- 세션 로그: `session_history/2026-07-19-551c3f-log.md`
- 진입 정본: `session_history/2026-07-16-mickey-power-kickoff.md` (§2 = 규약 전문)
- 결정 이력: `DECISIONS.md` (D-0717-1 / D-0717-2)
- hook/스크립트: `.kiro/hooks/`, `.kiro/scripts/` (git 추적 중)
