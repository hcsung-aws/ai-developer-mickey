# HANDOFF — 2026-07-17 [UID: b3d7e1] (mickey-power 트랙)

> 다음 세션 진입 문서. 이 트랙의 정체는 `session_history/2026-07-16-mickey-power-kickoff.md` 참조 (정적 진입 정본).
> 세션 페어: 본 HANDOFF의 상세 경위는 `session_history/2026-07-17-b3d7e1-log.md` 참조.

## Current Status (한 줄)

잔여 과제 1(.kiro gitignore 부분 해제) 완료 + IDE 1.0 hook 규격 조사 완료 + 세션 기록 규약(날짜+UID) 사용자 확정. 다음 세션은 규약 성문화 → hook 전면 개정 → IDE 런타임 실증 순으로 진행.

## 이번 세션 완료 사항 (전부 실측 검증됨)

1. **`.gitignore` 부분 해제** — `.kiro/` → `.kiro/*` + `!.kiro/hooks/` `!.kiro/scripts/`. hooks 4파일 + scripts 2파일 git 추적 전환, `settings/`는 계속 ignore (`git check-ignore` 확인). 커밋 `f9334c3` push 완료. clone 시 수동 복사 우회 소멸.
2. **IDE 1.0 hook 규격 조사** (공식 문서 기반, 아래 "확정 사실" 참조)
3. **세션 규약 분석 + 사용자 결정 확보** (아래 "확정된 결정" 참조)

## 확정된 결정 (사용자, 2026-07-17 — 다음 세션의 최우선 기준)

### D-0717-1: 세션 기록 규약 (날짜+UID)

배경: 같은 프로젝트에서 세션이 여러 개 동시에 열리는 IDE 상황 + CLI 연속성 공백(boot hook이 매번 "Mickey 1" 오판) 해소.

- **로그 파일명**: `session_history/YYYY-MM-DD-<UID>-log.md`
- **HANDOFF 파일명**: `session_history/YYYY-MM-DD-<UID>-handoff.md`
- **UID**: 6자리 hex 권장 (예: `b3d7e1`). session_history/ 내 유일성 확보. 생성 예: Python `uuid.uuid4().hex[:6]` (스크립트 파일로, one-liner 금지)
- **기록 후 의무**: 채팅 응답에 UID를 명시적으로 남긴다 (IDE는 지난 채팅을 열람 가능하므로 이것이 세션 선택의 열쇠)
- **이어가기 절차 (양쪽 분기)**:
  - **IDE**: 사용자가 새 세션에서 UID를 알려주면 → 해당 UID의 log/handoff 로드. UID 미지정 시 → CLI 경로와 동일하게 최신본 제시 후 확인.
  - **CLI**: 가장 최근 저장된 log + handoff를 탐지 → **사용자 확인 받은 뒤** 진행 (자동 진행 금지).
- 본 세션(b3d7e1)부터 즉시 적용 — 이 파일 자체가 첫 적용 사례.

### D-0717-2: hook 처리 방침

- 기존 hook은 **삭제 후 완전 개정** (부분 수정 아님).
- **IDE 런타임 실증은 규약 성문화 + hook 개정이 끝난 뒤** 수행.

## 다음 세션 작업 계획 (우선순위)

### 1순위 — 세션 규약 성문화

- `session_history/2026-07-16-mickey-power-kickoff.md` §2 개정: 날짜+UID 규약(위 D-0717-1 전문)으로 교체
- `DECISIONS.md`에 D-0717-1/D-0717-2 정식 기록 (Options/Chosen/Reasoning 스키마)
- 판단 필요: 이 규약을 이 디렉토리 한정으로 둘지, power steering(session-protocol.md)에 "MICKEY-N 사용 불가 환경" 분기로 일반화할지 — **일반화는 v3 통합 시점 판단으로 보류가 기본값** (사용자 재확인 전 steering 수정 금지)

### 2순위 — hook 삭제 + 완전 개정

삭제 대상 (전부 `.kiro/hooks/`):
- `mickey-pre-task.kiro.hook`, `mickey-post-task.kiro.hook` — 구/신 양쪽 규격에서 무효 (근거: 아래 확정 사실 #1·#2)
- `mickey-session-start.json` — 형식 자체는 신규격과 합치하나 boot 스크립트 개정과 함께 재작성

재설계 요구사항 (boot/close 스크립트 + hook JSON):
- **HANDOFF 탐지를 새 규약에 맞춤**: 현재 `mickey_session_boot.py` detect_handoff는 `session_history/*HANDOFF*.md` glob — 대문자 패턴이라 **Windows에서만** 소문자 `-handoff.md`에 매칭됨 (pathlib glob이 Windows에서 case-insensitive). 크로스 플랫폼 보장 위해 `*-handoff.md` 소문자 패턴으로 변경 + 최신본 선정 로직(mtime) 유지 + UID 파싱해 리포트에 표기
- **트랙 인지**: 이 디렉토리에서 "Mickey 1 세션으로 취급" guidance는 오판 — 날짜+UID 규약 기준 메시지로 교체 (P3 양쪽 분기 유지: handoff 존재 시 / 부재 시)
- **cp949 mojibake 해소**: hook stdout의 한글이 깨져 수신됨 (07-17 실측). `sys.stdout.reconfigure(encoding="utf-8")` 적용 검토 — 단, hook 소비자(CLI 런타임)의 기대 인코딩 실측 후 결정 (adaptive #8 계열)
- **verify_hooks.py 전면 개정**: 항목 2(session-stop 부재 검증)·항목 3(IDE skeleton 검증)이 삭제 대상과 결합돼 있음 — 검증기를 새 hook 구성 기준으로 재작성. **WELC**: 삭제 전 현재 통과 상태(6/6)를 기록해 두고, 개정 후 새 기준값 확립
- `.kiro/hooks/README.md` 개정 (폐기 이력 포함)
- close 스크립트(`mickey_session_close.py`)도 새 규약(날짜+UID handoff 생성 보조)에 맞춰 개정 검토

### 3순위 — IDE 런타임 실증 (1·2 완료 후, 사용자 IDE 필요)

체크리스트 초안:
- IDE 1.0에서 이 workspace 열기 → Agent Hooks 패널이 v1 JSON hook을 인식하는지
- `SessionStart` hook이 IDE에서 발화하고 stdout이 컨텍스트로 유입되는지
- power-mickey가 IDE Powers 패널에서 인식/활성화되는지 (keyword 트리거 + steering pull)
- 결과를 log에 기록하고 기준값化

## 확정 사실 (IDE 1.0 규격 — 공식 문서 실측, 2026-07-17)

1. **IDE 1.0은 legacy `.kiro.hook`(when/then)을 로딩하지 않는다.** 패널에 upgrade badge만 표시, 마이그레이션 전 미실행. 출처: kiro.dev/docs/whats-new-1-0.md
2. **신규격 = CLI v3와 동일한 v1 JSON**: `{"version":"v1","hooks":[{name, trigger, matcher, action, timeout, enabled}]}`. 트리거 10종 (SessionStart / Stop / PreToolUse / PostToolUse / PreTaskExec / PostTaskExec / UserPromptSubmit / PostFileCreate / PostFileSave / PostFileDelete). **IDE에도 SessionStart 존재** → hook 하나로 CLI/IDE 양쪽 커버 가능. 출처: kiro.dev/docs/hooks.md
3. exit code 시맨틱 동일: 0 → stdout이 컨텍스트 유입(SessionStart/UserPromptSubmit), 2 → 차단(PreToolUse/UserPromptSubmit/PreTaskExec)
4. Manual 트리거는 폐기 → manual steering 파일(슬래시 커맨드)로 대체
5. `DECISIONS.md` 2026-02-19 결정(userTriggered+askAgent)은 규격 폐기로 무효 — 성문화 시 Status 갱신 대상
6. powers/create.md 공식 문서에 구식 .kiro.hook 예시 잔존 (문서 lag — 규격 판단은 whats-new-1-0 기준)

## Important Context (log/kickoff에 없는 것)

- **이 환경(acp-client) 셸에서 `git commit -m "..."` 의 큰따옴표가 벗겨진다** — 메시지를 임시 파일로 쓰고 `git commit -F <file>` 사용 (07-17 2회 재현)
- boot hook은 acp-client에서도 발화 확인됨 (kickoff 문서의 "acp-client 미발화(V6)" 기록과 상충 — 클라이언트 버전업 추정, 개정 시 kickoff 문구 함께 갱신)
- `sessions/MICKEY-*` 파일들은 master(CLI 트랙) 소유 — 이 트랙에선 읽기 전용 참고. 이 트랙 기준 최신 인계는 본 handoff
- 검증 기준값 (07-17 실측): verify_power_structure 7/7, verify_hooks 6/6, verify_serving_sync IN-SYNC(9파일), check_disk_sync ALL-SYNCED
- Curator 호출은 이번 세션에서 **미수행** (사용자가 계획 작성+종료만 지시 — 세션 정리 full 절차 아님). auto_notes 미기록 관찰 2건은 log "관찰" 섹션에 있음 (mojibake, boot hook 트랙 미인지)

## Quick Reference

- 세션 로그: `session_history/2026-07-17-b3d7e1-log.md`
- 진입 정본: `session_history/2026-07-16-mickey-power-kickoff.md` (§2가 1순위 개정 대상)
- hook/스크립트: `.kiro/hooks/`, `.kiro/scripts/` (이제 git 추적됨)
- Context window (종료 시점): ~60% 추정 — 다음 세션은 fresh context 권장
